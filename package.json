{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "npm --prefix artifacts/redeemers-forge run dev",
    "build": "npm --prefix artifacts/redeemers-forge run build",
    "preview": "npm --prefix artifacts/redeemers-forge run preview",
    "typecheck": "npm --prefix artifacts/redeemers-forge run typecheck"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
